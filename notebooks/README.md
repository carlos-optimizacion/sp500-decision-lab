# Notebooks

Espacio reservado para exploración reproducible. Los resultados oficiales del MVP se generan con `scripts/build_snapshot.py`; un notebook no debe convertirse en una fuente paralela de lógica de producción.
